# Task 1 Brief: Scaffolding, Package Setup, and Mocking Mechanism

## Files
- Create: `package.json`
- Create: `repo-meta.json`
- Create: `.gitignore`

## Requirements
1. **package.json**: Write a Node.js package setup with standard scripts `build` and `build:mock`. Use type module.
2. **repo-meta.json**: Write a clean JSON containing default mapping override entries for at least `autostream` and `WindowLayout` keys.
3. **.gitignore**: Add `node_modules/`, `dist/`, and `.DS_Store`.
4. Install all dependencies (if any).

## Expected Output / Verifications
1. `npm install` runs and succeeds.
2. Files are successfully committed to Git under the commit message `chore: setup dependencies and initial files`.
